import { ChatDirectiveDirective } from './chat-directive.directive';

describe('ChatDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
